---
pageable: true
per_page: 10
---

<div class="blog-content">
  <div class="articles-list">
    <% page_articles.each_with_index do |article, i| %>
      <article class="article-list-item">
        <h2 class="article-list-item-title"><%= link_to article.title, article %></h2>
        <span class="article-list-item-meta"><%= article.date.strftime('%b %-d, %Y') %> • <%= article.data[:author] %></span>
      </article>
    <% end %>
  </div>
</div>

<div class="blog-sidebar security-updates">
  <h4>Security Updates</h4>

  <p>While we will post all security vulnerabilities to this blog you may wish to recieve notifications. For instant updates you can subscribe to the <a href="https://groups.google.com/forum/#!forum/solidus-security">Google group</a>.</p>
</div>

<% if paginate %>
  <ul class="pagination">
    <li class="button button-small button-outline <%= prev_page ? "" : "disabled" %>">
      <% if prev_page %>
        <%= link_to "Older Posts", prev_page %>
      <% else %>
        Older Posts
      <% end %>
    </li>
    <li class="button button-small button-outline <%= next_page ? "" : "disabled" %>">
      <% if next_page %>
        <%= link_to "Newer Posts", next_page %>
      <% else %>
        Newer Posts
      <% end %>
    </li>
  </ul>
<% end %>
